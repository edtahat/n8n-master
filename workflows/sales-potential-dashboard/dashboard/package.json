{
  "name": "sales-potential-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Dashboard React (standalone, bundle único) para o workflow n8n Sales Potential. Não faz parte do workspace pnpm do n8n — é buildado e inlinado manualmente no node 'Build Dashboard HTML' do workflow de webhook.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b",
    "embed": "node scripts/embed-into-workflow.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.15.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vite-plugin-singlefile": "^2.1.0"
  }
}
